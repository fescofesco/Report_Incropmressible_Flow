\documentclass[12pt,a4paper]{article}

% Packages
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{hyperref}
\usepackage{siunitx}
\usepackage{booktabs}
\usepackage{bm}
\usepackage{pdfpages}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, calc}
\usepackage{listings}
\usepackage{xcolor}

% Page geometry
\geometry{margin=2.5cm}

% Custom commands
\newcommand{\parder}[2]{\frac{\partial #1}{\partial #2}}
\newcommand{\pparder}[2]{\frac{\partial^2 #1}{\partial {#2}^2}}
\newcommand{\Rey}{\text{Re}}
\newcommand{\Pran}{\text{Pr}}

% Listings style for MATLAB
\definecolor{codegreen}{rgb}{0,0.5,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.5,0,0.35}
\definecolor{backcolour}{rgb}{0.97,0.97,0.97}
\definecolor{matlabblue}{rgb}{0.0,0.0,0.55}

\lstdefinestyle{matlabstyle}{
    backgroundcolor=\color{backcolour},
    commentstyle=\color{codegreen}\itshape,
    keywordstyle=\color{matlabblue}\bfseries,
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=4,
    language=Matlab,
    frame=single,
    rulecolor=\color{codegray},
}
\lstset{style=matlabstyle}

\begin{document}

% ============================================================================
% TITLE PAGE
% ============================================================================
\begin{titlepage}
    \centering
    \vspace*{2cm}

    {\Huge\bfseries Development of Flow at the Entrance of a Cylindrical Pipe\par}

    \vspace{1.5cm}

    {\Large Numerical Simulation and Modelling of Incompressible Flow\par}

    \vspace{1cm}

    {\large LV-Nr.: 321.053 EX\par}
    {\large Winter Semester 2025/26\par}

    \vfill

    {\Large
    Felix Scope\\
    \texttt{felix\_scope@yahoo.de}\\
    }

    \vfill

    {\large \today\par}
\end{titlepage}

\tableofcontents
\newpage

% ============================================================================
% ASSIGNMENT
% ============================================================================
\section*{Assignment}
\addcontentsline{toc}{section}{Assignment}
\includepdf[pages=-]{../Assignemnt/Assignment_WS25.pdf}

\newpage

% ============================================================================
% STATEMENT ON THE USE OF AI TOOLS
% ============================================================================
\section*{Statement on the Use of AI Tools}
\addcontentsline{toc}{section}{Statement on the Use of AI Tools}

AI-based tools (an LLM coding assistant, Claude) were used while preparing both the MATLAB
solver and this report: for drafting and debugging portions of the MATLAB code, for typesetting
and organising sections of the report and several of its schematic figures, and for wording and
proofreading assistance.

Every governing equation, discretisation choice, boundary condition, and numerical result
presented here was worked through, checked, and is understood by the author. The AI tool was
used as an implementation and editing aid -- to speed up typing, catch mistakes, and keep the
document consistent -- not as a substitute for the underlying engineering judgement: the
derivation of the equations, the finite-volume discretisation and projection method, the choice
of numerical schemes, and the interpretation of the results were all reasoned through and can be
explained and defended independently of the tool, as required at the individual oral assessment.

\newpage

% ============================================================================
% 1. PROBLEM DESCRIPTION
% ============================================================================
\section{Problem Description}

\subsection{Geometry and Setup}

The problem considers two-dimensional laminar flow in a cylindrical pipe with diameter~$D$ and length $L = 50D$.
The pipe wall is heated with constant heat flux $q_w = \text{const}$.
The incompressible fluid has constant properties: density~$\rho$, dynamic viscosity~$\mu$, thermal conductivity~$\lambda$, and specific heat capacity~$c_v$.
Viscous dissipation is neglected.

The flow enters with uniform velocity and temperature profiles:
\begin{align}
    w\big|_{z=0} &= W_{\text{in}} = \text{const.} \\
    T\big|_{z=0} &= T_{\text{in}} = \text{const.}
\end{align}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{../Heun/Plots_python/geometry_diagram.png}
    \caption{Schematic of the cylindrical pipe flow with constant wall heat flux.
             Flow enters with uniform velocity $W_{\text{in}}$ and temperature $T_{\text{in}}$,
             and develops into a parabolic profile along the pipe length.}
    \label{fig:geometry}
\end{figure}

\subsection{Physical Parameters}

\begin{itemize}
    \item Reynolds number: $\Rey = \dfrac{\rho W_{\text{in}} D}{\mu} = 100$
    \item Prandtl number: $\Pran = \dfrac{\mu c_v}{\lambda} = 5$
    \item Length-to-diameter ratio: $L/D = 50$
\end{itemize}

\newpage

% ============================================================================
% 2. MATHEMATICAL FORMULATION
% ============================================================================
\section{Mathematical Formulation}

\subsection{Governing Equations in Cylindrical Coordinates}

For axisymmetric flow in cylindrical coordinates $(r, z)$ with velocity components $(u, w)$ in the radial and axial directions, the governing equations in \textbf{conservation form} are:

\subsubsection{Continuity Equation}

\begin{equation}
    \underbrace{\frac{1}{r}\parder{(r\,u)}{r}}_{\text{radial mass-flux divergence}}
    + \underbrace{\parder{w}{z}}_{\text{axial mass-flux divergence}} = 0
    \label{eq:continuity_dim}
\end{equation}
The $1/r$ weighting on the radial term accounts for the circumferential area
$2\pi r$ growing with radius; the two terms summing to zero enforces
incompressibility (no net accumulation of mass in any control volume).

\subsubsection{Momentum Equations}

\textbf{Radial momentum:}
\begin{equation}
    \parder{u}{t} + \frac{1}{r}\parder{(r\,u^2)}{r} + \parder{(u\,w)}{z}
    = -\frac{1}{\rho}\parder{p}{r}
      + \frac{1}{\rho}\left[\frac{1}{r}\parder{(r\,\tau_{rr})}{r} - \frac{\tau_{\varphi\varphi}}{r} + \parder{\tau_{rz}}{z}\right]
    \label{eq:momentum_r_dim}
\end{equation}

\textbf{Axial momentum:}
\begin{equation}
    \parder{w}{t} + \frac{1}{r}\parder{(r\,u\,w)}{r} + \parder{(w^2)}{z}
    = -\frac{1}{\rho}\parder{p}{z}
      + \frac{1}{\rho}\left[\frac{1}{r}\parder{(r\,\tau_{rz})}{r} + \parder{\tau_{zz}}{z}\right]
    \label{eq:momentum_z_dim}
\end{equation}

Term by term, each momentum equation reads (local rate of change) + (convection)
= (pressure force) + (viscous force):
\begin{itemize}
    \item $\parder{u}{t}$, $\parder{w}{t}$ --- \textbf{local (unsteady) acceleration}: the rate of
          change of momentum stored in a fixed control volume.
    \item $\frac{1}{r}\parder{(r\,u^2)}{r}$, $\parder{(u\,w)}{z}$ (and the analogous axial terms)
          --- \textbf{convective transport}: momentum carried in and out of the control volume by
          the flow itself (advection by $u$ and $w$).
    \item $-\frac{1}{\rho}\parder{p}{r}$, $-\frac{1}{\rho}\parder{p}{z}$ --- \textbf{pressure-gradient
          force} per unit mass, the driving force pushing fluid from high to low pressure.
    \item $\frac{1}{\rho}\big[\frac1r\parder{(r\tau_{rr})}{r}+\parder{\tau_{rz}}{z}\big]$ and the
          axial counterpart --- \textbf{viscous (diffusive) force} per unit mass, the net effect
          of internal friction spreading momentum from fast- to slow-moving fluid.
    \item $-\dfrac{\tau_{\varphi\varphi}}{\rho r}$ --- a \textbf{curvature (``hoop-stress'') term}
          unique to cylindrical coordinates: it arises because the circumferential stress
          $\tau_{\varphi\varphi}$ acts on a surface whose area changes with $r$; there is no
          Cartesian analogue.
\end{itemize}

The stress tensor components for a Newtonian fluid are:
\begin{equation}
    \underbrace{\tau_{rr} = 2\mu\parder{u}{r}}_{\text{radial normal stress}}, \quad
    \underbrace{\tau_{\varphi\varphi} = 2\mu\frac{u}{r}}_{\text{hoop (circumferential) normal stress}}, \quad
    \underbrace{\tau_{zz} = 2\mu\parder{w}{z}}_{\text{axial normal stress}}, \quad
    \underbrace{\tau_{rz} = \mu\left(\parder{u}{z} + \parder{w}{r}\right)}_{\text{shear stress}}
\end{equation}

\subsubsection{Energy Equation}

\begin{equation}
    \underbrace{\parder{T}{t}}_{\text{local rate of change}}
    + \underbrace{\frac{1}{r}\parder{(r\,u\,T)}{r} + \parder{(w\,T)}{z}}_{\text{convective transport of heat by the flow}}
    = \underbrace{\frac{\lambda}{\rho c_v}\left[\frac{1}{r}\parder{}{r}\!\left(r\parder{T}{r}\right) + \pparder{T}{z}\right]}_{\text{conductive (diffusive) transport, Fourier's law}}
    \label{eq:energy_dim}
\end{equation}
Viscous dissipation (heat generated by internal friction) is neglected, per the problem statement.

\subsection{Boundary Conditions}

\textbf{Inlet} ($z = 0$):
\begin{equation}
    u = 0, \quad w = W_{\text{in}}, \quad T = T_{\text{in}}
\end{equation}

\textbf{Wall} ($r = D/2$):
\begin{equation}
    u = 0, \quad w = 0, \quad \lambda\parder{T}{r}\bigg|_{r=D/2} = q_w
\end{equation}
Heat flows into the fluid at the wall (constant heating), so with $r$ increasing outward this is a positive gradient: the wall is hotter than the centreline.

\textbf{Centreline} ($r = 0$, symmetry):
\begin{equation}
    u = 0, \quad \parder{w}{r}\bigg|_{r=0} = 0, \quad \parder{T}{r}\bigg|_{r=0} = 0
\end{equation}

\textbf{Outlet} ($z = L$, zero-gradient):
\begin{equation}
    \parder{u}{z} = 0, \quad \parder{w}{z} = 0, \quad \parder{T}{z} = 0
\end{equation}

\newpage

\subsection{Non-Dimensionalization}

\subsubsection{Reference Scales}
\begin{itemize}
    \item Length: $D$
    \item Velocity: $W_{\text{in}}$
    \item Pressure: $\rho W_{\text{in}}^2$
    \item Temperature difference: $\dfrac{q_w}{\rho c_v W_{\text{in}}}$
    \item Time: $D / W_{\text{in}}$
\end{itemize}

\subsubsection{Non-Dimensional Variables}
\begin{align}
    r^* &= \frac{r}{D}, \quad z^* = \frac{z}{D}, \quad t^* = \frac{t\,W_{\text{in}}}{D} \\[6pt]
    u^* &= \frac{u}{W_{\text{in}}}, \quad w^* = \frac{w}{W_{\text{in}}}, \quad
    p^* = \frac{p}{\rho W_{\text{in}}^2} \\[6pt]
    \theta &= \frac{(T - T_{\text{in}})\,\rho\, c_v\, W_{\text{in}}}{q_w}
\end{align}

\subsubsection{Non-Dimensional Governing Equations}

Dropping asterisks for brevity (all quantities are non-dimensional hereafter):

\textbf{Continuity:}
\begin{equation}
    \frac{1}{r}\parder{(r\,u)}{r} + \parder{w}{z} = 0
    \label{eq:continuity_nd}
\end{equation}

\textbf{Radial momentum:}
\begin{equation}
    \parder{u}{t} + \frac{1}{r}\parder{(r\,u^2)}{r} + \parder{(u\,w)}{z}
    = -\parder{p}{r}
      + \frac{1}{\Rey}\left[\frac{1}{r}\parder{}{r}\!\left(r\parder{u}{r}\right) - \frac{u}{r^2} + \pparder{u}{z}\right]
    \label{eq:momentum_r_nd}
\end{equation}

\textbf{Axial momentum:}
\begin{equation}
    \parder{w}{t} + \frac{1}{r}\parder{(r\,u\,w)}{r} + \parder{(w^2)}{z}
    = -\parder{p}{z}
      + \frac{1}{\Rey}\left[\frac{1}{r}\parder{}{r}\!\left(r\parder{w}{r}\right) + \pparder{w}{z}\right]
    \label{eq:momentum_z_nd}
\end{equation}

\textbf{Energy:}
\begin{equation}
    \parder{\theta}{t} + \frac{1}{r}\parder{(r\,u\,\theta)}{r} + \parder{(w\,\theta)}{z}
    = \frac{1}{\Rey\Pran}\left[\frac{1}{r}\parder{}{r}\!\left(r\parder{\theta}{r}\right) + \pparder{\theta}{z}\right]
    \label{eq:energy_nd}
\end{equation}

\subsubsection{Non-Dimensional Boundary Conditions}

\textbf{Inlet} ($z = 0$):
$\;u = 0,\; w = 1,\; \theta = 0$

\textbf{Wall} ($r = 1/2$):
$\;u = 0,\; w = 0,\; \partial\theta/\partial r = \Rey\Pran$

Non-dimensionalizing the wall condition directly: $\partial\theta/\partial r^* = D\cdot(\rho c_v W_{\text{in}}/q_w)\cdot\partial T/\partial r|_{\text{wall}} = D\cdot(\rho c_v W_{\text{in}}/q_w)\cdot(q_w/\lambda) = \rho c_v W_{\text{in}} D/\lambda = \Rey\Pran$ (i.e.\ 500 for $\Rey=100,\Pran=5$), \emph{not} $\pm 1$ — the reference length/temperature scales do not cancel.

\textbf{Centreline} ($r = 0$):
$\;u = 0,\; \partial w/\partial r = 0,\; \partial\theta/\partial r = 0$

\textbf{Outlet} ($z = 50$):
$\;\partial u/\partial z = 0,\; \partial w/\partial z = 0,\; \partial\theta/\partial z = 0$

\newpage

% ============================================================================
% 3. NUMERICAL SOLUTION
% ============================================================================
\section{Numerical Solution}

\subsection{Computational Domain and Grid}

The computational domain is:
\begin{equation}
    \Omega = \{(r, z) : 0 \leq r \leq 0.5, \quad 0 \leq z \leq 50\}
\end{equation}

A \textbf{staggered grid} (MAC grid) is used:
\begin{itemize}
    \item Scalar variables ($p$, $\theta$): cell centres $(r_{c,i},\, z_{c,j})$
    \item Radial velocity $u$: radial faces $(r_{f,i},\, z_{c,j})$
    \item Axial velocity $w$: axial faces $(r_{c,i},\, z_{f,j})$
\end{itemize}

Grid parameters: $n_r = 50$ cells radially, $n_z = 250$ cells axially, giving:
\begin{equation}
    \Delta r = \frac{0.5}{n_r} = 0.01, \qquad \Delta z = \frac{50}{n_z} = 0.2
\end{equation}

\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=1.5, >=Latex]

% ---------------------------------------------------------------
% Front face (theta = theta_j): the (r,z) cross-section of one CV
% ---------------------------------------------------------------
\coordinate (A) at (0,0);       % r_i,     z_j
\coordinate (B) at (2.6,0);     % r_i,     z_{j+1}
\coordinate (C) at (2.6,1.8);   % r_{i+1}, z_{j+1}
\coordinate (D) at (0,1.8);     % r_{i+1}, z_j

% Sweep (theta-direction) offsets. Larger radius -> larger projected
% displacement for the same physical d(theta), since arc length = r*dtheta.
\coordinate (dIn)  at (0.70,0.42);
\coordinate (dOut) at (1.10,0.66);

\coordinate (Ap) at ($(A)+(dIn)$);
\coordinate (Bp) at ($(B)+(dIn)$);
\coordinate (Cp) at ($(C)+(dOut)$);
\coordinate (Dp) at ($(D)+(dOut)$);

% ---- back (hidden) rectangle ----
\draw[gray!55,thin] (Ap) -- (Bp) -- (Cp) -- (Dp) -- cycle;

% ---- sweep edges: r=const cylindrical surfaces, drawn as gentle arcs ----
\draw[gray!65,thin]  (A) to[bend left=8]  (Ap);
\draw[gray!65,thin]  (B) to[bend left=8]  (Bp);
\draw[black!75,thin] (D) to[bend left=14] (Dp);
\draw[black!75,thin] (C) to[bend left=14] (Cp);

% ---- front face: the control volume, highlighted ----
\fill[green!22] (A) -- (B) -- (C) -- (D) -- cycle;
\draw[black,very thick] (A) -- (B) -- (C) -- (D) -- cycle;

% ---- scalar node p,theta at cell centre ----
\coordinate (P) at ($(A)!0.5!(C)$);
\filldraw[blue] (P) circle (2.2pt);
\node[blue,font=\small,above right=1pt] at (P) {$p_{i,j},\,\theta_{i,j}$};

% ---- u (radial velocity) on the r-faces, red -- both point in +r ----
\coordinate (uIn)  at ($(A)!0.5!(B)$);
\coordinate (uOut) at ($(D)!0.5!(C)$);
\draw[red,thick,->] ($(uIn)+(0,-0.22)$)  -- ($(uIn)+(0,0.20)$);
\node[red,font=\small,below] at ($(uIn)+(0,-0.24)$) {$u_{i,j}$};
\draw[red,thick,->] ($(uOut)+(0,-0.20)$) -- ($(uOut)+(0,0.22)$);
\node[red,font=\small,above] at ($(uOut)+(0,0.24)$) {$u_{i+1,j}$};

% ---- w (axial velocity) on the z-faces, teal -- both point in +z ----
\coordinate (wLeft)  at ($(A)!0.5!(D)$);
\coordinate (wRight) at ($(B)!0.5!(C)$);
\draw[teal,thick,->] ($(wLeft)+(-0.20,0)$)  -- ($(wLeft)+(0.22,0)$);
\node[teal,font=\small,left] at ($(wLeft)+(-0.22,0)$) {$w_{i,j}$};
\draw[teal,thick,->] ($(wRight)+(-0.22,0)$) -- ($(wRight)+(0.20,0)$);
\node[teal,font=\small,right] at ($(wRight)+(0.22,0)$) {$w_{i,j+1}$};

% ---- r_i, r_{i+1} labels with leader lines to the curved (r=const) edges ----
\draw[gray!70] (-0.55,-0.55) -- ($(A)!0.5!(Ap)$);
\node[font=\small] at (-0.75,-0.7) {$r_i$};

\draw[gray!70] (-0.55,2.35) -- ($(D)!0.5!(Dp)$);
\node[font=\small] at (-0.75,2.5) {$r_{i+1}$};

% ---- z_j, z_{j+1} labels ----
\node[font=\small] at (0,-0.35) {$z_j$};
\node[font=\small] at (2.6,-0.35) {$z_{j+1}$};

% ---- dr, dz dimension arrows ----
\draw[<->] (-1.15,0) -- (-1.15,1.8) node[midway,left,font=\small] {$\Delta r$};
\draw[<->] (0,-0.85) -- (2.6,-0.85) node[midway,below,font=\small] {$\Delta z$};

% ---- d(theta) angle indicator on the outer sweep (right of the CV, clear of labels) ----
\draw[purple!70,densely dashed] (C) -- ($(C)+(1.0,0)$);
\draw[purple!70,densely dashed] (Cp) -- ($(C)+(1.0,0)$);
\draw[purple!70] ($(C)+(0.55,0)$) arc[start angle=0, end angle=31, radius=0.55];
\node[purple!70,font=\small] at ($(C)+(0.85,0.22)$) {$\Delta\theta$};

% ---- small r,z,theta orientation triad ----
\begin{scope}[shift={(4.6,0.2)}]
    \draw[->] (0,0) -- (0,0.7) node[above]{$r$};
    \draw[->] (0,0) -- (0.9,0) node[right]{$z$};
    \draw[->] (0,0) to[bend left=25] (0.35,0.32) node[right]{$\theta$};
\end{scope}

% ---- legend ----
\begin{scope}[shift={(4.4,1.55)}]
    \filldraw[blue] (0,0.55) circle (2.2pt);
    \node[font=\small,right] at (0.15,0.55) {$p,\,\theta$ (cell centre)};
    \draw[red,thick,->] (0,0.15) -- (0,0.4);
    \node[font=\small,right] at (0.15,0.28) {$u$ ($r$-face)};
    \draw[teal,thick,->] (0,-0.15) -- (0.25,-0.15);
    \node[font=\small,right] at (0.35,-0.15) {$w$ ($z$-face)};
\end{scope}

\end{tikzpicture}
\caption{Staggered grid in the $(r,z)$ plane. Scalars ($p$, $\theta$) at the cell centre
         $(r_{c,i}, z_{c,j})$ (blue); radial velocity $u$ on the (curved, cylindrical)
         $r$-faces (red), stored at $u_{i,j}$ and $u_{i+1,j}$; axial velocity $w$ on the
         $z$-faces (teal), stored at $w_{i,j}$ and $w_{i,j+1}$. The control volume is
         drawn swept through a finite $\Delta\theta$ purely to convey the 3-D cylindrical
         shape of the $r$-faces (note the outer face at $r_{i+1}$ sweeps further than
         the inner face at $r_i$, since arc length $=r\,\Delta\theta$); the axisymmetric
         problem itself has $\partial/\partial\theta = 0$ and no $\theta$-dependence.}
    \label{fig:grid_structure}
\end{figure}

\subsection{Spatial Discretization: Finite Volume Method}

The governing equations are integrated over each control volume using the FVM.
After applying Gauss' divergence theorem, the volume integrals become surface flux integrals that are evaluated at control-volume faces.

\subsubsection{Continuity (at scalar CV)}

\begin{equation}
    \frac{r_{i+\frac{1}{2}}\,u_{i+1,j} - r_{i-\frac{1}{2}}\,u_{i,j}}{r_{c,i}\,\Delta r}
    + \frac{w_{i,j+1} - w_{i,j}}{\Delta z} = 0
    \label{eq:div_discrete}
\end{equation}

\subsubsection{Convective Scheme: 2nd-Order Upwind (Linear Upwind Differencing)}

Central differencing requires the cell Peclet number $\text{Pe} = |v|\,\Delta x / (\nu_{\text{eff}})$ to satisfy $\text{Pe} < 2$.
For our grid:
\begin{equation}
    \text{Pe}_z = |w|\,\Delta z\,\Rey = 1 \times 0.2 \times 100 = 20 \gg 2
\end{equation}
(and $\text{Pe}_\theta = |w|\,\Delta z\,\Rey\Pran = 100$ for the energy equation, even higher).

Pure central differencing is therefore prone to non-physical oscillations at these cell Peclet
numbers. We use \textbf{2nd-order upwind (linear-upwind / LUD) differencing} for convective
terms: at each face, the transported quantity is linearly extrapolated from the two cells on
the \textit{upwind} side of the face, based on the sign of the transporting velocity. On a
uniform grid and for a smooth solution, this face reconstruction has a truncation error of
$\mathcal{O}(\Delta x^2)$, so the convective discretization is formally second-order accurate,
as required by the assignment. This is an accuracy statement, not a boundedness guarantee:
because the extrapolated face value is not limited, LUD can overshoot or undershoot near sharp
transients, boundaries, or insufficiently resolved gradients. Thus the scheme is second-order
in smooth regions but is not monotone or TVD everywhere.

For a generic flux $F = v\,\phi$ at the face between cells $i$ and $i+1$:
\begin{equation}
    \phi_{\text{face}} =
    \begin{cases}
        \tfrac{3}{2}\phi_i - \tfrac{1}{2}\phi_{i-1} & \text{if } v_{\text{face}} > 0 \\[4pt]
        \tfrac{3}{2}\phi_{i+1} - \tfrac{1}{2}\phi_{i+2} & \text{if } v_{\text{face}} < 0
    \end{cases}
\end{equation}
which reduces to the classic 1st-order upwind value ($\phi_i$ or $\phi_{i+1}$) if the linear
extrapolation term is dropped.

Diffusive terms retain standard \textbf{second-order central differencing}.

\paragraph{Why these reconstructions are second order.} Expand $\phi$ in a Taylor series about
the face location $x_f$ (face between cells $i$ and $i{+}1$, spacing $\Delta x$, upwind case
$v_{\text{face}}>0$): with $x_i = x_f-\tfrac12\Delta x$ and $x_{i-1}=x_f-\tfrac32\Delta x$,
\begin{equation}
    \phi_i = \phi_f - \tfrac12\Delta x\,\phi_f' + \tfrac18\Delta x^2\phi_f'' + \mathcal{O}(\Delta x^3),
    \qquad
    \phi_{i-1} = \phi_f - \tfrac32\Delta x\,\phi_f' + \tfrac98\Delta x^2\phi_f'' + \mathcal{O}(\Delta x^3).
\end{equation}
Plain 1st-order upwind ($\phi_{\text{face}}=\phi_i$) retains the $-\tfrac12\Delta x\,\phi_f'$ term
and is therefore only $\mathcal{O}(\Delta x)$ accurate. Forming the linear-upwind combination
$\tfrac32\phi_i-\tfrac12\phi_{i-1}$ cancels the $\phi_f'$ term \emph{exactly}
($-\tfrac32\cdot\tfrac12\Delta x+\tfrac12\cdot\tfrac32\Delta x=0$), leaving
$\phi_{\text{face}}=\phi_f-\tfrac38\Delta x^2\phi_f''+\mathcal{O}(\Delta x^3)$: the leading error is
$\mathcal{O}(\Delta x^2)$, confirming LUD is second order. The central-difference diffusion
operator is second order by the same mechanism applied symmetrically: expanding $\phi_{i\pm1}$
about $x_i$ and summing, the odd-order ($\Delta x$, $\Delta x^3$) terms cancel by symmetry,
leaving $(\phi_{i+1}-2\phi_i+\phi_{i-1})/\Delta x^2 = \phi_i''+\tfrac{1}{12}\Delta x^2\phi_i''''+\mathcal{O}(\Delta x^4)$.

\subsubsection{Temperature equation and cell-by-cell development}

The temperature variable $\theta_{i,j}$ is stored at the centre of every scalar control
volume. At each time step, its value is advanced from the net transport and diffusion through
the four faces of that control volume:
\begin{equation}
        \frac{\mathrm{d}\theta_{i,j}}{\mathrm{d}t}
        = -\frac{1}{r_{c,i}\Delta r}
            \left[(r u\theta)_e-(r u\theta)_w\right]
            -\frac{(w\theta)_n-(w\theta)_s}{\Delta z}
            +\frac{1}{\Rey\Pran}\left[
            \frac{r_{i+\frac12}\theta_r|_e-r_{i-\frac12}\theta_r|_w}{r_{c,i}\Delta r}
            +\frac{\theta_z|_n-\theta_z|_s}{\Delta z}\right].
        \label{eq:temperature_cell_balance}
\end{equation}
The convective face temperatures are reconstructed with the LUD formula above, while the
diffusive face gradients use central differences. The wall face receives the prescribed heat
flux through the ghost value
$\theta_g=\theta_{i,j}+\Delta r\,\Rey\Pran$; the inlet face uses $\theta=0$ and the
outlet uses a zero axial gradient. Thus every cell is updated from its local radial and axial
neighbour values, together with the local face velocities. In the AB2 time integrator the
resulting cellwise right-hand side is stored and updated as
\begin{equation}
        \theta_{i,j}^{n+1}=\theta_{i,j}^{n}+\Delta t
        \left(\tfrac32 R_{\theta,i,j}^{n}-\tfrac12 R_{\theta,i,j}^{n-1}\right),
\end{equation}
with a forward-Euler startup step when the previous right-hand side is unavailable. Heat
therefore first enters cells adjacent to the wall and then spreads radially and downstream;
the centreline and downstream cells develop later because they receive heat through diffusion
and axial advection rather than directly from the heated wall.

\subsubsection{Axial Momentum (at w-face CV)}

The w-CV at $(r_{c,i},\, z_{f,j})$ spans $[r_{f,i},\, r_{f,i+1}] \times [z_{c,j-1},\, z_{c,j}]$.

\textbf{AB2 predictor step} (including the known pressure gradient):
\begin{equation}
\boxed{
    w^*_{i,j} = w^n_{i,j} + \Delta t\left[
    \frac{3}{2}F^n_{w,i,j}-\frac{1}{2}F^{n-1}_{w,i,j}
    -\frac{p^n_{i,j}-p^n_{i,j-1}}{\Delta z}\right],
    \quad F_w=-C_r^w-C_z^w+D_r^w+D_z^w
}
\label{eq:predictor_w}
\end{equation}

where the convective terms (upwind) are:
\begin{align}
    C_r^w &= \frac{r_{i+\frac{1}{2}}\,u_E\,w_{E,\text{up}} - r_{i-\frac{1}{2}}\,u_I\,w_{I,\text{up}}}{r_{c,i}\,\Delta r} \\
    C_z^w &= \frac{\bar{w}_N\,w_{N,\text{up}} - \bar{w}_S\,w_{S,\text{up}}}{\Delta z}
\end{align}

and the diffusive terms (central) are:
\begin{align}
    D_r^w &= \frac{1}{\Rey}\,\frac{r_{i+\frac{1}{2}}(w_{i+1,j}-w_{i,j}) - r_{i-\frac{1}{2}}(w_{i,j}-w_{i-1,j})}{r_{c,i}\,\Delta r^2} \\
    D_z^w &= \frac{1}{\Rey}\,\frac{w_{i,j+1} - 2w_{i,j} + w_{i,j-1}}{\Delta z^2}
\end{align}

The radial momentum equation is discretized analogously at u-face control volumes.

\subsection{Temporal Discretization}

Explicit second-order Adams--Bashforth time stepping is used. For a spatial
operator $F$, the update is
\begin{align}
    F^{AB2} &= \tfrac{3}{2}F^n-\tfrac{1}{2}F^{n-1}, \\
    \phi^{n+1} &= \phi^n+\Delta t\,F^{AB2}.
\end{align}
The first step uses forward Euler because $F^{n-1}$ is not yet available. The
current momentum and temperature RHS arrays are stored for the following step.
AB2 needs one RHS evaluation and one pressure-Poisson solve per time step.

\paragraph{Why AB2 is second order.} Forward Euler, $\phi^{n+1}=\phi^n+\Delta t\,F^n$, freezes the
right-hand side at its value at $t^n$ and so approximates the average slope over $[t^n,t^{n+1}]$
with $\mathcal{O}(\Delta t)$ error, giving local truncation error $\mathcal{O}(\Delta t^2)$ per
step and global error $\mathcal{O}(\Delta t)$. AB2 instead linearly extrapolates the two most
recent slopes. Expanding $F^{n-1}=F(t^n-\Delta t) = F^n-\Delta t\,\dot F^n+\tfrac12\Delta t^2\ddot
F^n+\mathcal{O}(\Delta t^3)$,
\begin{equation}
    \tfrac32 F^n-\tfrac12 F^{n-1}
    = F^n+\tfrac12\Delta t\,\dot F^n-\tfrac14\Delta t^2\ddot F^n+\mathcal{O}(\Delta t^3),
\end{equation}
which matches the true average slope on $[t^n,t^{n+1}]$, $F^n+\tfrac12\Delta t\,\dot F^n
+\mathcal{O}(\Delta t^2)$, through the $\mathcal{O}(\Delta t)$ term: the leading error in the
slope estimate is pushed from $\mathcal{O}(\Delta t^0)$ (Euler) to $\mathcal{O}(\Delta t^2)$.
Multiplying by $\Delta t$ in the update gives local truncation error $\mathcal{O}(\Delta t^3)$
per step; accumulating over the $N=\mathcal{O}(1/\Delta t)$ steps needed to reach a fixed final
time gives global error $\mathcal{O}(\Delta t^2)$, i.e.\ AB2 is second order. Because the
pressure-Poisson equation is solved directly (Section~\ref{sec:poisson_solver}, no
iterative truncation) at every step, it introduces no additional error beyond machine precision;
combined with the $\mathcal{O}(\Delta x^2)$ spatial operators above, the overall scheme is
therefore second order in \emph{both} space and time, as the assignment requires.

The temperature equation uses the same AB2 extrapolation of its
convection--diffusion operator.

The time step $\Delta t = 0.002$ satisfies the stability constraints:
\begin{itemize}
    \item Convective CFL: $\Delta t / \Delta z = 0.01 \ll 1$
    \item Viscous stability: $\Delta t\,(1/\Rey)\,(1/\Delta r^2 + 1/\Delta z^2) \approx 0.2 < 0.5$
\end{itemize}

\subsection{Pressure Correction Method (Projection)}
\label{sec:pressure-correction}

A segregated approach is used. Each time step consists of:

\begin{enumerate}
    \item \textbf{Predictor}: Extrapolate the convective--diffusive momentum
    RHS with AB2 and include the known pressure gradient:
    \begin{equation}
        \vec{u}^{*}=\vec{u}^{n}+\Delta t\left[
        \tfrac{3}{2}F^n-\tfrac{1}{2}F^{n-1}-\nabla p^n\right].
    \end{equation}

    \item \textbf{Poisson equation}: Find pressure correction $p'$ such that the corrected velocity is divergence-free:
    \begin{equation}
        \frac{1}{r}\parder{}{r}\!\left(r\parder{p'}{r}\right) + \pparder{p'}{z}
        = \frac{1}{\Delta t}\left[\frac{1}{r}\parder{(r\,u^*)}{r} + \parder{w^*}{z}\right]
        \label{eq:poisson}
    \end{equation}

    Discretized:
    \begin{equation}
        \frac{r_{i+\frac{1}{2}}(p'_{i+1,j}-p'_{i,j}) - r_{i-\frac{1}{2}}(p'_{i,j}-p'_{i-1,j})}{r_{c,i}\,\Delta r^2}
        + \frac{p'_{i,j+1} - 2p'_{i,j} + p'_{i,j-1}}{\Delta z^2}
        = b_{i,j}
    \end{equation}

    \item \textbf{Velocity correction}:
    \begin{align}
        u^{n+1}_{i,j} &= u^*_{i,j} - \Delta t\,\frac{p'_{i,j} - p'_{i-1,j}}{\Delta r} \\
        w^{n+1}_{i,j} &= w^*_{i,j} - \Delta t\,\frac{p'_{i,j} - p'_{i,j-1}}{\Delta z}
    \end{align}

    \item \textbf{Pressure update} with under-relaxation:
    \begin{equation}
        p^{n+1} = p^n + \alpha_p\,p', \qquad \alpha_p = 0.7
    \end{equation}
\end{enumerate}

\subsection{Poisson Solver}
\label{sec:poisson_solver}

The Poisson equation~\eqref{eq:poisson} is assembled into a sparse linear system $\mathbf{A}\,\mathbf{p}' = \mathbf{b}$ of size $(n_r \cdot n_z) \times (n_r \cdot n_z)$.
The matrix~$\mathbf{A}$ is assembled once and reused every time step.
It is solved using MATLAB's built-in \textbf{direct sparse solver} (backslash operator \texttt{A\textbackslash b}), which employs LU decomposition for the sparse system.

Boundary conditions for $p'$:
\begin{itemize}
    \item Centreline ($r=0$): Neumann $\partial p'/\partial r = 0$ (natural: $r_{-\frac{1}{2}} = 0$, flux vanishes)
    \item Wall ($r=0.5$): Neumann $\partial p'/\partial r = 0$
    \item Inlet ($z=0$): Neumann $\partial p'/\partial z = 0$
    \item Outlet ($z=50$): Dirichlet $p' = 0$ is imposed at the last pressure cell centre as the reference. This location is half a cell upstream of the physical outlet face, so the condition is a discrete pressure reference rather than an exactly face-centred outlet condition.
\end{itemize}

\subsection{Ghost Cells and Boundary Implementation}

Ghost cells are used to impose the velocity and temperature boundary conditions in the staggered grid.
Pressure-correction boundary conditions are imposed in the sparse Poisson matrix; the outlet
reference is applied at the last pressure cell centre rather than through a pressure ghost cell.

\begin{table}[H]
\centering
\caption{Ghost cell values for each boundary.}
\label{tab:ghost}
\begin{tabular}{lllll}
\toprule
\textbf{Boundary} & $u_{\text{ghost}}$ & $w_{\text{ghost}}$ & $p_{\text{ghost}}$ & $\theta_{\text{ghost}}$ \\
\midrule
Centreline ($r=0$) & $u_g = 0$ & $w_g = w_1$ (symmetry) & $p_g = p_1$ & $\theta_g = \theta_1$ \\
Wall ($r=0.5$) & $u_g = 0$ & $w_g = -w_{n_r}$ (no-slip) & $p_g = p_{n_r}$ & $\theta_g = \theta_{n_r} + \Delta r\cdot\Rey\Pran$ \\
Inlet ($z=0$) & $u_g = -u_1$ & $w = 1$ (Dirichlet) & $p_g = p_1$ & $\theta = 0$ (Dirichlet) \\
Outlet ($z=L$) & $u_g = u_{n_z}$ & $w_g = w_{n_z}$ & $p' = 0$ & $\theta_g = \theta_{n_z}$ \\
\bottomrule
\end{tabular}
\end{table}

The wall temperature ghost cell $\theta_g = \theta_{n_r} + \Delta r\cdot\Rey\Pran$ implements the Neumann condition $\partial\theta/\partial r = \Rey\Pran$ via central differencing:
\begin{equation}
    \parder{\theta}{r}\bigg|_{\text{wall}} \approx \frac{\theta_g - \theta_{n_r}}{\Delta r} = \frac{\Delta r\cdot\Rey\Pran}{\Delta r} = \Rey\Pran
\end{equation}
An earlier version of this derivation incorrectly dropped the $\Rey\Pran$ factor (giving $\partial\theta/\partial r = -1$); see \texttt{Report/temperature\_formula\_review.md} for the full re-derivation and cross-checks.

\newpage

% ============================================================================
% 4. IMPLEMENTATION
% ============================================================================
\section{Implementation}

\subsection{Code Structure}

The solver is implemented in MATLAB.
Table~\ref{tab:modules} lists all functions and their roles.

\begin{table}[H]
\centering
\caption{MATLAB function structure.}
\label{tab:modules}
\begin{tabular}{lp{8.5cm}}
\toprule
\textbf{Function} & \textbf{Description} \\
\midrule
\texttt{constants.m}          & Returns struct with physical parameters ($\Rey$, $\Pran$), grid sizes, time step, tolerances \\
\texttt{make\_grid.m}         & Generates staggered grid: $r_c$, $r_f$, $z_c$, $z_f$, $\Delta r$, $\Delta z$ \\
\texttt{initialise\_fields.m} & Allocates fields ($u$, $w$, $p$, $\theta$) with uniform initial conditions \\
\texttt{apply\_bc\_w.m}       & Enforces axial velocity BCs (inlet Dirichlet, outlet zero-gradient) \\
\texttt{apply\_bc\_u.m}       & Enforces radial velocity BCs (axis and wall: $u=0$) \\
\texttt{apply\_bc\_T.m}       & Enforces temperature BCs (inlet Dirichlet, outlet zero-gradient) \\
\texttt{apply\_all\_bc.m}     & Dispatches to \texttt{apply\_bc\_u/w/T.m} in one call (used for the initial-condition BCs before the time loop) \\
\texttt{upwind2\_face.m}      & 2nd-order upwind (linear-extrapolation) face-value helper, shared by all three RHS functions below \\
\texttt{compute\_rhs\_w.m}    & Explicit RHS of axial momentum (no pressure): 2nd-order upwind convection, central diffusion \\
\texttt{compute\_rhs\_u.m}    & Explicit RHS of radial momentum (no pressure): 2nd-order upwind convection, central diffusion \\
\texttt{compute\_rhs\_T.m}    & Explicit RHS of the energy equation: 2nd-order upwind convection, central diffusion \\
\texttt{predictor\_w.m}       & Thin 1-stage-Euler wrapper around \texttt{compute\_rhs\_w.m} (kept for standalone use) \\
\texttt{predictor\_u.m}       & Thin 1-stage-Euler wrapper around \texttt{compute\_rhs\_u.m} (kept for standalone use) \\
\texttt{build\_poisson\_matrix.m} & Assembles sparse Poisson matrix $\mathbf{A}$ (called once) \\
\texttt{solve\_poisson.m}     & Solves $\mathbf{A}\,\mathbf{p}' = \mathbf{b}$ using MATLAB backslash \\
\texttt{correct\_velocity.m}  & Applies pressure-gradient correction to $u^*$, $w^*$ \\
\texttt{update\_pressure.m}   & Updates pressure: $p^{n+1} = p^n + \alpha_p\,p'$ \\
\texttt{advance\_temperature.m} & Thin 1-stage-Euler wrapper around \texttt{compute\_rhs\_T.m} (kept for standalone use) \\
\texttt{ab2\_step.m}          & Advances $(u,w,p,\theta)$ with AB2 and one incremental pressure correction; stores the current RHS for the next step \\
\texttt{compute\_divergence.m}   & Computes discrete divergence of velocity field \\
\texttt{compute\_residuals.m}    & Computes convergence residuals ($R_{\text{cont}}$, $R_{\text{vel}}$, $R_{\theta}$) \\
\texttt{check\_convergence.m}    & Returns true when all residuals are below tolerances \\
\texttt{analytical\_velocity.m}  & Hagen--Poiseuille velocity profile \\
\texttt{analytical\_temperature.m} & Analytical temperature profile for constant wall heat flux \\
\texttt{main.m}               & Main script: initialisation $\to$ time stepping $\to$ post-processing and plotting \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Algorithm Flowchart}

Figure~\ref{fig:flowchart} shows the algorithm implemented in \texttt{main.m}.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0.6cm,
    startstop/.style={rectangle, rounded corners, minimum width=4.5cm, minimum height=0.8cm,
                      text centered, draw=black, fill=blue!10, font=\small},
    process/.style={rectangle, minimum width=5.5cm, minimum height=0.8cm,
                    text centered, draw=black, fill=orange!10, font=\small},
    decision/.style={diamond, aspect=2.5, minimum width=3cm, minimum height=0.8cm,
                     text centered, draw=black, fill=green!10, font=\small},
    io/.style={trapezium, trapezium left angle=70, trapezium right angle=110,
               minimum width=4cm, minimum height=0.8cm,
               text centered, draw=black, fill=yellow!10, font=\small},
    arrow/.style={thick, -Stealth},
    loop/.style={rectangle, rounded corners, draw=gray, dashed, inner sep=0.4cm}
]

% Nodes
\node (start) [startstop] {Start};
\node (grid) [process, below=of start] {Generate staggered grid};
\node (init) [process, below=of grid] {Initialise fields ($u$, $w$, $p$, $\theta$)};
\node (poisson_mat) [process, below=of init] {Assemble Poisson matrix $\mathbf{A}$ (once)};
\node (bc0) [process, below=of poisson_mat] {Apply boundary conditions};

\node (pred) [process, below=1.5cm of bc0] {AB2 predictor $w^*$, $u^*$ including $\nabla p^n$};
\node (solve_p) [process, below=of pred] {Solve Poisson: $\mathbf{A}\,\mathbf{p}' = \mathbf{b}$};
\node (correct) [process, below=of solve_p] {Correct: $u^{n+1}$, $w^{n+1}$, $p^{n+1}$};
\node (temp) [process, below=of correct] {Advance temperature $\theta^{n+1}$};
\node (bc1) [process, below=of temp] {Apply boundary conditions};
\node (conv) [decision, below=0.6cm of bc1] {Converged?};

\node (output) [io, below=0.8cm of conv] {Generate plots and save results};
\node (stop) [startstop, below=of output] {End};

% Time loop box
\node[loop, fit=(pred)(solve_p)(correct)(temp)(bc1)(conv)] (timeloop) {};
\node[font=\small\bfseries, anchor=south west, xshift=0.05cm, yshift=0.1cm]
    at (timeloop.north west) {Time loop ($n = 1, \ldots, n_{\max}$)};

% Arrows
\draw[arrow] (start) -- (grid);
\draw[arrow] (grid) -- (init);
\draw[arrow] (init) -- (poisson_mat);
\draw[arrow] (poisson_mat) -- (bc0);
\draw[arrow] (bc0) -- (pred);
\draw[arrow] (pred) -- (solve_p);
\draw[arrow] (solve_p) -- (correct);
\draw[arrow] (correct) -- (temp);
\draw[arrow] (temp) -- (bc1);
\draw[arrow] (bc1) -- (conv);
\draw[arrow] (conv) -- node[right, font=\small] {Yes} (output);
\draw[arrow] (conv.east) -- ++(2.6,0) node[pos=0.5, above, font=\small] {No} |- (pred.east);
\draw[arrow] (output) -- (stop);

\end{tikzpicture}
\caption{Algorithm flowchart for the pipe flow solver.}
\label{fig:flowchart}
\end{figure}

\subsection{Key Implementation Details}

\begin{itemize}
    \item \textbf{2nd-order upwind differencing}: Convective fluxes in both momentum and energy equations use LUD. It is formally second-order on smooth, resolved regions, as required by the assignment, but it is unbounded and can produce small local overshoots near sharp transients or boundaries.

    \item \textbf{Sparse direct solver}: The Poisson matrix ($12{,}500 \times 12{,}500$) is assembled once using MATLAB's \texttt{sparse} function and solved each time step with the backslash operator (\texttt{A\textbackslash b}).

    \item \textbf{Vectorised operations}: All stencil computations are performed using MATLAB's array operations, avoiding explicit loops over grid cells for efficiency.

    \item \textbf{Staggered grid}: Prevents checkerboard pressure oscillations; velocity components are stored at the faces where mass fluxes are naturally evaluated.

    \item \textbf{Ghost cells}: Velocity and temperature boundary conditions are implemented through ghost-cell extensions. Pressure-correction boundary conditions are imposed directly in the sparse Poisson matrix, including the last-cell-centre outlet reference.
\end{itemize}

\newpage

% ============================================================================
% 5. ANALYTICAL SOLUTION
% ============================================================================
\section{Analytical Solution}

For fully developed flow (large $z$), analytical solutions exist for comparison.

\subsection{Velocity Profile (Hagen--Poiseuille)}

\begin{equation}
    \frac{w}{W_{\text{in}}} = 2\left[1 - (2r^*)^2\right]
    \label{eq:analytical_velocity}
\end{equation}

The centreline velocity is $w_{\max} = 2\,W_{\text{in}}$.
The hydrodynamic entry length is approximately $L_h \approx 0.05\,\Rey\,D = 5D$.

\subsection{Temperature Profile}

For constant wall heat flux in fully developed thermal flow:
\begin{equation}
    \theta = 4z^* + \Rey\Pran\left[\frac{1}{2}(2r^*)^2 - \frac{1}{8}(2r^*)^4 - \frac{7}{48}\right]
    \label{eq:analytical_temperature}
\end{equation}
This form follows from direct integration of the non-dimensional energy
equation~\eqref{eq:energy_nd} in the fully developed regime (Section~\ref{sec:nusselt-check}),
subject to the wall condition $\lambda\,\partial T/\partial r|_{\text{wall}}=q_w$ and the global
energy balance $\theta_{\text{bulk}}(z^*) = 4z^*$; see Section~\ref{sec:nusselt-check} for the
full derivation and literature sources.

The thermal entry length is $L_t \approx 0.05\,\Rey\,\Pran\,D = 25D$.

\subsection{Validity of the Fully-Developed Temperature Profile}
\label{sec:fd-validity}

Equation~\eqref{eq:analytical_temperature} makes two logically separate claims, which hold over
different ranges of $z^*$ because they rest on different physics: one about the
cross-sectional \emph{level} of the temperature (the bulk value), and one about its \emph{radial
shape}.

\paragraph{The bulk temperature is exact from the inlet.}
The bulk (mixing-cup) temperature is the flow-weighted cross-sectional average
\begin{equation}
    \theta_{\text{bulk}}(z^*) =
        \frac{\displaystyle\int w^*\,\theta \; \mathrm{d}A}{\displaystyle\int w^* \, \mathrm{d}A}.
\end{equation}
Its axial evolution follows from an energy balance on a thin slice of pipe --- enthalpy gained
by the stream equals heat entering through the wall:
\begin{equation}
    \dot{m}\,c_v\,\parder{T_{\text{bulk}}}{z} = q_w\,\pi D .
\end{equation}
Every factor on the right is constant ($q_w$ is constant and applied from $z=0$; the perimeter
is fixed), and $\dot m$, $c_v$ are constant, so $\partial T_{\text{bulk}}/\partial z$ is constant
along the \emph{entire} pipe. Non-dimensionalized this is $\partial\theta_{\text{bulk}}/\partial
z^* = 4$, and integrating from $\theta_{\text{bulk}}(0)=0$ gives
\begin{equation}
    \theta_{\text{bulk}}(z^*) = 4z^* \qquad\text{exactly, for all } z^*\ge 0 ,
\end{equation}
developed or not. This balance involves only totals (total wall heat input, total enthalpy
flux); it never refers to the shape of the radial profile, so the thermal entrance region does
not perturb it. The one effect that would --- axial conduction near the inlet --- is neglected
here, as specified.

\paragraph{The radial shape requires thermally developed flow.}
Reducing the energy equation~\eqref{eq:energy_nd} to the closed $r^*$-form
in~\eqref{eq:analytical_temperature} assumes the profile is \emph{thermally fully developed}:
self-similar in shape, merely translating downstream, so
$\partial(\theta-\theta_{\text{bulk}})/\partial z^* = 0$. That is exactly the step that makes
$\partial\theta/\partial z^*$ the $r^*$-independent constant $4$ and collapses the PDE to an ODE
in $r^*$. Near the inlet it is false: at $z^*=0$ the temperature is uniform ($\theta=0$, flat);
the developed shape has the wall hotter than the centreline by $\tfrac{11}{48}\Rey\Pran$; and
over the thermal entry length $L_t\approx 25D$ the profile morphs continuously between the two,
a thin heated layer growing inward from the wall while the core stays at inlet temperature.
Through this region $\partial(\theta-\theta_{\text{bulk}})/\partial z^*$ is large and
$r^*$-dependent, and the ODE solution does not apply. Evaluated there anyway,
\eqref{eq:analytical_temperature} gives at the centreline
$4z^* + \Rey\Pran(-\tfrac{7}{48}) \approx 4z^* - 73$, i.e.\ negative for $z^*\lesssim 18$ ---
claiming the core is colder than the inlet. The numerical solution instead holds the core near
$\theta=0$ until the thermal layer reaches it ($z^*\approx 25$), then bends toward the analytical
curve, the two nearly coinciding by $z^*=50$ (Fig.~\ref{fig:T_profiles}). By contrast the
hydrodynamic entry length is only $L_h\approx 0.05\,\Rey\,D \approx 5D$: velocity develops about
five times sooner, since here momentum diffuses $\Pran=5$ times faster than heat.

\paragraph{Why the level can be right while the shape is not.}
The constant $-\tfrac{7}{48}$ is fixed by requiring the flow-weighted average of the radial
bracket to vanish, so \eqref{eq:analytical_temperature} is \emph{built} to satisfy
$\theta_{\text{bulk}}=4z^*$; its mean is pinned to the correct value even where its distribution
about that mean is wrong. This mirrors the Hagen--Poiseuille
profile~\eqref{eq:analytical_velocity}, which always carries the correct flow rate (mean
$w^*=1$ by construction) although the true profile near the inlet is plug-like, not parabolic
--- right integral, wrong shape. The fully-developed profile is therefore used only as the
$z^*\!\to\!\infty$ reference it is: its departure from the numerical solution at small $z^*$ is
expected behaviour, not a defect, and resolving the entrance region is the numerical solution's
role.

\subsection{Nusselt Number}
\label{sec:nusselt-check}

For hydrodynamically and thermally fully developed laminar flow in a circular pipe with
\emph{constant wall heat flux}, the Nusselt number is the exact rational value
\begin{equation}
    \text{Nu} = \frac{h D}{\lambda} = \frac{q_w D}{\lambda\,(T_{\text{wall}} - T_{\text{bulk}})}
              = \frac{\Rey\Pran}{\theta_{\text{wall}} - \theta_{\text{bulk}}}
              = \frac{48}{11} \approx 4.3636 ,
    \label{eq:nusselt}
\end{equation}
a standard textbook result for this thermal boundary condition
\cite{ShahLondon1978,Incropera}.

\paragraph{Derivation.} In the fully developed region $u^*=0$ and the profile translates
rigidly in $z^*$, so the non-dimensional energy equation~\eqref{eq:energy_nd} reduces to
\begin{equation}
    w^*(r^*)\,\parder{\theta}{z^*}
      = \frac{1}{\Rey\Pran}\,\frac{1}{r^*}\parder{}{r^*}\!\left(r^*\parder{\theta}{r^*}\right).
    \label{eq:fd_energy}
\end{equation}
A global energy balance over a pipe slice fixes the axial gradient: with the wall adding heat at
non-dimensional rate $\partial\theta/\partial r^*|_{\text{wall}}=\Rey\Pran$ and the flow carrying
mean velocity $\langle w^*\rangle = 1$, the bulk (mixing-cup) temperature satisfies
$\theta_{\text{bulk}}(z^*) = 4z^*$ exactly, hence $\partial\theta/\partial z^* = 4$ everywhere in
the cross-section. Substituting this and $w^*=2[1-(2r^*)^2]$ into~\eqref{eq:fd_energy} and
integrating twice, with $\partial\theta/\partial r^*|_{r^*=0}=0$ and
$\partial\theta/\partial r^*|_{r^*=1/2}=\Rey\Pran$,
\begin{equation}
    \theta(r^*,z^*) = 4z^* + \Rey\Pran\left[\tfrac12(2r^*)^2 - \tfrac18(2r^*)^4 + c\right] ,
\end{equation}
and the constant $c$ follows from requiring the flow-weighted average of the bracket to vanish
(so that $\theta_{\text{bulk}}=4z^*$), which gives $c=-\tfrac{7}{48}$ --- recovering
Eq.~\eqref{eq:analytical_temperature}. Evaluating the bracket at the wall gives
$\theta_{\text{wall}}-\theta_{\text{bulk}} = \tfrac{11}{48}\,\Rey\Pran$, and~\eqref{eq:nusselt}
follows.

\paragraph{Consistency check.} The value is exact: \eqref{eq:fd_energy} with its boundary
conditions is a linear problem with no free parameters, so $\text{Nu}$ is independent of $\Rey$,
$\Pran$, $q_w$, the fluid properties and the grid. Taking instead the assignment sheet's literal
radial coefficients $(2r^*)^2-\tfrac14(2r^*)^4-\tfrac34$ yields
$\theta_{\text{wall}}-\theta_{\text{bulk}} = \tfrac{11}{24}\,\Rey\Pran$ and hence
$\text{Nu}=24/11\approx2.18$ --- exactly half of $48/11$, and inconsistent both with this
classical result and with the assignment's own stated wall condition (that literal profile has
wall gradient $2\,\Rey\Pran$, and its bulk temperature is not $4z^*$).

\paragraph{Empirical check against the CFD solution.} The solver imposes the physically
consistent wall condition $\partial\theta/\partial r^*=\Rey\Pran$. Computing the flow-weighted
bulk temperature from the converged field ($t^*=139.728$) and forming
$\text{Nu}=\Rey\Pran/(\theta_{\text{wall}}-\theta_{\text{bulk}})$ at three downstream stations:

\begin{center}
\begin{tabular}{cccccc}
\toprule
$z^*$ & settling margin $t^*-z^*$ & $\theta_{\text{bulk}}$ (CFD) & $4z^*$ (expected) & $\text{Nu}$ (CFD) & classical $48/11$ \\
\midrule
29.9 & 30.1 & 119.43 & 119.6 & 4.585 & 4.364 \\
39.9 & 20.1 & 155.82 & 159.6 & 4.727 & 4.364 \\
48.9 & 11.1 & 181.79 & 195.6 & 5.048 & 4.364 \\
\bottomrule
\end{tabular}
\end{center}

The global balance $\theta_{\text{bulk}}\approx4z^*$ holds to $\sim\!1\%$ at $z^*=29.9$ (largest
settling margin), and the CFD $\text{Nu}$ is within $\sim\!5\%$ of $48/11$ there, degrading
toward the outlet exactly as expected from progressively less settling time (thermal diffusion
across the radius has non-dimensional timescale $\sim\Rey\Pran R^{*2}\approx125$, comparable to
the simulated horizon). This is a model-free confirmation, built only from the governing PDE and
the wall BC, that the physical answer is near $48/11$ and not near the $2.18$ implied by the
assignment-literal coefficients.

\paragraph{Why the solver can never reproduce $24/11$.} The energy equation, the wall BC, and the
inlet condition $\theta_{\text{in}}=0$ are all \emph{linear} in $\theta$, and $\text{Nu}$ in
\eqref{eq:nusselt} is normalised by the wall flux. Multiplying the imposed wall flux by any
constant $k$ simply scales the entire $\theta$ field by $k$, leaving $\text{Nu}$ unchanged at
$48/11$. A CFD run therefore cannot reproduce $24/11$: that number exists only as a property of
the internally inconsistent literal formula, never of a correctly posed simulation
\cite{ShahLondon1978,Incropera,KaysCrawford,BirdStewartLightfoot}.

\newpage

% ============================================================================
% 6. RESULTS
% ============================================================================
\section{Results}

\subsection{Simulation Parameters}

\begin{table}[H]
\centering
\caption{Simulation parameters.}
\label{tab:params}
\begin{tabular}{lll}
\toprule
\textbf{Parameter} & \textbf{Value} & \textbf{Description} \\
\midrule
$\Rey$ & 100 & Reynolds number \\
$\Pran$ & 5 & Prandtl number \\
$n_r \times n_z$ & $50 \times 250$ & Grid resolution \\
$\Delta r$ & 0.01 & Radial grid spacing \\
$\Delta z$ & 0.2 & Axial grid spacing \\
$\Delta t$ & 0.002 & Time step \\
$n_{\text{steps}}$ & 30\,000 & Maximum time steps \\
$\alpha_p$ & 0.7 & Pressure under-relaxation \\
Convective scheme & 2nd-order upwind (LUD) & Stability for Pe $\gg 2$; formally 2nd-order on smooth, resolved fields \\
Time integration & Explicit Adams--Bashforth 2 & 2nd-order; Euler startup \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Convergence History}

The simulation targets convergence when the continuity, velocity, and temperature residuals fall
below $10^{-6}$:
\begin{equation}
    R_{\text{cont}} = \max_{i,j}|\nabla\cdot\mathbf{u}|, \quad
    R_{\text{vel}} = \frac{1}{\Delta t}\max\!\left(
        \max|u^{n+1}-u^n|,\max|w^{n+1}-w^n|\right), \quad
    R_{\theta} = \frac{\max|\theta^{n+1}-\theta^n|}{\Delta t}
\end{equation}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{../AdamsBashfort2/Plots_matlab/convergence.png}
    \caption{Convergence history: residuals versus time step.}
    \label{fig:convergence}
\end{figure}

The continuity residual (blue) is flat from the very first step rather than decaying over time:
the incremental pressure correction re-solves the Poisson equation to machine precision every
single step, so $\nabla\cdot\mathbf{u}$ is driven to its numerical floor ($\sim10^{-14}$--$10^{-15}$)
at each iteration by construction, independent of whether the flow itself has reached steady
state. $R_{\text{vel}}$ (red), in contrast, measures how much the velocity field is still
changing between steps: it decays exponentially, by roughly one decade every $1000$ steps
($\Delta t^*=2$), until it reaches the round-off floor at step $\approx15{,}000$
($t^*\approx30$) -- only then does it flatten into a noisy plateau rather than continuing
smoothly to zero. That plateau is round-off noise: $R_{\text{vel}}$ divides an already
near-machine-precision difference
$u^{n+1}-u^n$ by $\Delta t=0.002$, amplifying floating-point noise by a factor $1/\Delta t=500$,
which is why it appears jagged at $\mathcal{O}(10^{-13})$ while $R_{\text{cont}}$ (not divided by
$\Delta t$) stays visually flat near $\mathcal{O}(10^{-14})$--$\mathcal{O}(10^{-15})$.

In the extended MATLAB run, $R_{\text{cont}}=6.46\times10^{-15}$ and
$R_{\text{vel}}=2.78\times10^{-14}$ at the converged time $t^*=139.728$. The temperature residual
decreases from approximately $101$ at the first step and reaches
$R_\theta=9.99\times10^{-7}$ at step $69{,}864$, just below the target $10^{-6}$. This residual is
the maximum local rate of change, not the error relative to the analytical temperature profile. Its
earlier non-monotone decrease was therefore not a numerical failure: as the heated layer moved
downstream and diffused inward, the cell containing the largest change moved as well. The dominant
timescale is thermal diffusion across the radius, $\Rey\Pran R^{*2}\approx125$, which explains why
the temperature required substantially longer to settle than the velocity. The final result is
therefore a converged finite-volume solution at the stated residual tolerance, while the analytical
fully-developed temperature profile remains a separate asymptotic reference for radial shape.

\subsection{Velocity Field}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{../AdamsBashfort2/Plots_matlab/w_contour.png}
    \caption{Axial velocity contour $w/W_{\text{in}}$ with velocity vectors throughout the
             domain. The flow transitions from a uniform inlet profile ($w=1$) to a parabolic
             profile ($w_{\max}=2$ at centreline); the arrows show the axial and radial
             components of the velocity field at regularly spaced cell centres.}
    \label{fig:w_contour}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{../AdamsBashfort2/Plots_matlab/u_contour.png}
    \caption{Radial velocity contour $u/W_{\text{in}}$.
             Non-zero only in the entrance region where the flow is developing; vanishes as the flow becomes fully developed.}
    \label{fig:u_contour}
\end{figure}

\subsection{Temperature Field}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{../AdamsBashfort2/Plots_matlab/theta_contour.png}
    \caption{Non-dimensional temperature contour $\theta$.
             Temperature increases along the pipe due to the constant wall heat flux.}
    \label{fig:theta_contour}
\end{figure}

\subsection{Velocity Profiles at Selected Axial Positions}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{../AdamsBashfort2/Plots_matlab/w_profiles.png}
    \caption{Axial velocity profiles $w(r)$ at $z^* = 5, 10, 20, 30, 40, 50$.
             Dashed lines show the analytical Hagen--Poiseuille profile.
             The velocity profile develops from uniform at the inlet to parabolic downstream.}
    \label{fig:w_profiles}
\end{figure}

\subsection{Temperature Profiles at Selected Axial Positions}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{../AdamsBashfort2/Plots_matlab/T_profiles.png}
    \caption{Non-dimensional temperature profiles $\theta(r)$ at selected axial positions
             (solid, numerical). The dashed curve is the analytical fully-developed
             profile~\eqref{eq:analytical_temperature}, overlaid only at the largest scalar
             location ($z^*=49.9$). The numerical curve has the expected developed radial shape;
             any remaining difference from the dashed curve reflects comparison with a
             fully-developed asymptotic radial profile and the finite axial location. The dashed
             curve is a \emph{fully-developed asymptote}
             (Section~\ref{sec:fd-validity}):
             only meant to be reached for increasing $z^*$, not a valid prediction at every $z^*$
             --- evaluated at small $z^*$ (e.g.\ $z^*=4.9$) it would extrapolate to strongly
             negative $\theta$ near the centreline (the $4z^*$ term not yet compensating for the
             formula's negative radial offset). The final numerical field is essentially
             non-negative; a small, localized transient LUD undershoot occurs near the
             inlet--wall corner during the first time steps. Hence the asymptotic curve is not
             plotted at those smaller-$z^*$ positions.}
    \label{fig:T_profiles}
\end{figure}

\subsection{Comparison with Analytical Solution}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{../AdamsBashfort2/Plots_matlab/w_comparison.png}
    \caption{Velocity profile at the last cell-centre location, $z^*=49.9$ (nominally the
             $z^*=50$ outlet, fully developed): numerical vs.\ analytical
             Hagen--Poiseuille solution. The absolute-error minimum near $r^*\approx0.43$
             occurs where the signed difference between the numerical and analytical profiles
             changes sign. Conservation fixes the numerical area-mean velocity through the
             imposed inlet flow rate, which agrees with the continuous Hagen--Poiseuille mean;
             consequently, a small negative core error is compensated by a small positive
             near-wall error. The discrete cell-centre means differ slightly because of radial
             quadrature. The subsequent rise toward the wall is associated with local wall and
             cylindrical-curvature discretization, wall ghost-cell treatment, and profile
             sampling, not with a physical velocity minimum.}
    \label{fig:w_comparison}
\end{figure}

\newpage

% ============================================================================
% 7. CONCLUSION
% ============================================================================
\section{Conclusion}

A finite volume solver for 2D axisymmetric incompressible pipe flow was developed in MATLAB and validated against analytical solutions.
The solver uses a staggered grid with 2nd-order upwind (linear upwind) differencing for convective stability, explicit second-order Adams--Bashforth time integration, and an incremental projection method with one sparse direct Poisson solve per time step.

The numerical results reproduce the expected physics:
\begin{itemize}
    \item The velocity field develops from a uniform inlet profile to the parabolic Hagen--Poiseuille profile within the hydrodynamic entry length. At $z^*=50$ the numerical and analytical
          Hagen--Poiseuille profiles agree to within $\mathcal{O}(10^{-4})$ (Fig.~\ref{fig:w_comparison}) -- the velocity field is essentially fully converged and validated.
    \item The temperature field shows the characteristic linear rise of the bulk temperature along the pipe axis ($\theta_{\text{bulk}}\approx4z^*$, confirmed to within $\sim$1\% at $z^*=29.9$; see
          Section~\ref{sec:nusselt-check}) and a radial profile that approaches the analytical fully-developed solution as $z^*$ increases (Fig.~\ref{fig:T_profiles}), as expected since that solution
          is only valid asymptotically. Agreement is closest where the flow has had the most settling time; at smaller $z^*$ the difference is primarily the thermal entrance-region shape, while the final field satisfies the temporal convergence criterion.
\end{itemize}

\newpage

% ============================================================================
% APPENDIX A: FULL FVM DERIVATION
% ============================================================================
\appendix

% The following appendix provides the complete FVM derivation including
% projection method, Poisson solver procedure, and ghost-cell BCs, for the
% (r,z) axisymmetric problem that was actually solved. The Cartesian (x,y)
% and cylindrical (r,phi) derivations are supplementary (requested by the
% assignment brief as general derivation exercises, not part of the solved
% problem) and are collected separately in Supplementary_Derivations.pdf.
\input{2_2D_cylindrical_coordinates_uw}
\label{app:derivation}

\newpage

% ============================================================================
% APPENDIX B: MATLAB SOURCE CODE
% ============================================================================
\section{MATLAB Source Code}
\label{app:code}

The complete MATLAB source code for the AB2 solver is listed below. Each function
corresponds to a module in Table~\ref{tab:modules}.

\subsection{Configuration and Initialisation}
\lstinputlisting[caption={constants.m --- Physical parameters, grid size, and solver tolerances}, label={lst:constants}]{../AdamsBashfort2/src_matlab/constants.m}
\lstinputlisting[caption={initialise\_fields.m --- Allocation and initial conditions for all fields}, label={lst:initfields}]{../AdamsBashfort2/src_matlab/initialise_fields.m}

\subsection{Main Simulation Script}
\lstinputlisting[caption={main.m --- Main simulation loop and plotting}, label={lst:main}]{../AdamsBashfort2/src_matlab/main.m}

\subsection{Grid Generation}
\lstinputlisting[caption={make\_grid.m --- Staggered grid generation}, label={lst:grid}]{../AdamsBashfort2/src_matlab/make_grid.m}

\subsection{Boundary Conditions}
\lstinputlisting[caption={apply\_all\_bc.m --- Driver applying all field boundary conditions}, label={lst:bcall}]{../AdamsBashfort2/src_matlab/apply_all_bc.m}
\lstinputlisting[caption={apply\_bc\_w.m --- Axial velocity boundary conditions}, label={lst:bcw}]{../AdamsBashfort2/src_matlab/apply_bc_w.m}
\lstinputlisting[caption={apply\_bc\_u.m --- Radial velocity boundary conditions}, label={lst:bcu}]{../AdamsBashfort2/src_matlab/apply_bc_u.m}
\lstinputlisting[caption={apply\_bc\_T.m --- Temperature boundary conditions}, label={lst:bcT}]{../AdamsBashfort2/src_matlab/apply_bc_T.m}

\subsection{Momentum Solver}
\lstinputlisting[caption={predictor\_w.m --- Axial velocity predictor}, label={lst:predw}]{../AdamsBashfort2/src_matlab/predictor_w.m}
\lstinputlisting[caption={predictor\_u.m --- Radial velocity predictor}, label={lst:predu}]{../AdamsBashfort2/src_matlab/predictor_u.m}
\lstinputlisting[caption={upwind2\_face.m --- 2nd-order upwind (linear-extrapolation) face-value helper}, label={lst:upwind2}]{../AdamsBashfort2/src_matlab/upwind2_face.m}
\lstinputlisting[caption={compute\_rhs\_w.m --- Axial momentum RHS: 2nd-order upwind convection, central diffusion}, label={lst:rhsw}]{../AdamsBashfort2/src_matlab/compute_rhs_w.m}
\lstinputlisting[caption={compute\_rhs\_u.m --- Radial momentum RHS: 2nd-order upwind convection, central diffusion}, label={lst:rhsu}]{../AdamsBashfort2/src_matlab/compute_rhs_u.m}
\lstinputlisting[caption={ab2\_step.m --- AB2 step with one incremental pressure correction}, label={lst:ab2}]{../AdamsBashfort2/src_matlab/ab2_step.m}

\subsection{Poisson Solver and Pressure Correction}
\lstinputlisting[caption={build\_poisson\_matrix.m --- Sparse Poisson matrix assembly}, label={lst:buildA}]{../AdamsBashfort2/src_matlab/build_poisson_matrix.m}
\lstinputlisting[caption={solve\_poisson.m --- Poisson solve using MATLAB backslash}, label={lst:solveP}]{../AdamsBashfort2/src_matlab/solve_poisson.m}
\lstinputlisting[caption={compute\_divergence.m --- Discrete velocity divergence (Poisson RHS and continuity residual)}, label={lst:divergence}]{../AdamsBashfort2/src_matlab/compute_divergence.m}
\lstinputlisting[caption={correct\_velocity.m --- Velocity correction step}, label={lst:correct}]{../AdamsBashfort2/src_matlab/correct_velocity.m}
\lstinputlisting[caption={update\_pressure.m --- Additive pressure-correction update}, label={lst:updatep}]{../AdamsBashfort2/src_matlab/update_pressure.m}

\subsection{Temperature Solver}
\lstinputlisting[caption={compute\_rhs\_T.m --- Energy equation RHS: 2nd-order upwind convection, central diffusion}, label={lst:rhsT}]{../AdamsBashfort2/src_matlab/compute_rhs_T.m}
\lstinputlisting[caption={advance\_temperature.m --- Temperature time advance}, label={lst:advT}]{../AdamsBashfort2/src_matlab/advance_temperature.m}

\subsection{Convergence Monitoring}
\lstinputlisting[caption={compute\_residuals.m --- Residual computation}, label={lst:residuals}]{../AdamsBashfort2/src_matlab/compute_residuals.m}
\lstinputlisting[caption={check\_convergence.m --- Convergence check}, label={lst:convergence}]{../AdamsBashfort2/src_matlab/check_convergence.m}

\subsection{Analytical Reference Solutions}
\lstinputlisting[caption={analytical\_velocity.m --- Hagen--Poiseuille axial velocity profile}, label={lst:anavel}]{../AdamsBashfort2/src_matlab/analytical_velocity.m}
\lstinputlisting[caption={analytical\_temperature.m --- Fully developed temperature profile ($\text{Nu}=48/11$)}, label={lst:anatemp}]{../AdamsBashfort2/src_matlab/analytical_temperature.m}

\subsection{Plotting Helper}
\lstinputlisting[caption={diverging\_bwr.m --- Blue--white--red diverging colormap for contour plots}, label={lst:bwr}]{../AdamsBashfort2/src_matlab/diverging_bwr.m}

\newpage

% ============================================================================
% REFERENCES
% ============================================================================
\begin{thebibliography}{9}

\bibitem{ShahLondon1978}
R.~K. Shah and A.~L. London,
\emph{Laminar Flow Forced Convection in Ducts},
Supplement~1 to \emph{Advances in Heat Transfer}, Academic Press, New York, 1978.
(Circular duct, H1 boundary condition: $\text{Nu} = 48/11$.)

\bibitem{Incropera}
F.~P. Incropera and D.~P. DeWitt,
\emph{Fundamentals of Heat and Mass Transfer},
Wiley, New York.
(Internal flow chapter: fully developed laminar flow in a circular tube with uniform surface
heat flux, $\text{Nu}_D = 48/11 \approx 4.36$.)

\bibitem{KaysCrawford}
W.~M. Kays, M.~E. Crawford, and B.~Weigand,
\emph{Convective Heat and Mass Transfer},
McGraw-Hill, New York.
(Fully developed laminar circular-tube flow, constant heat rate per unit length:
$\text{Nu} = 48/11$.)

\bibitem{BirdStewartLightfoot}
R.~B. Bird, W.~E. Stewart, and E.~N. Lightfoot,
\emph{Transport Phenomena},
Wiley, New York.
(The same fully developed constant-wall-flux tube problem is worked out and yields
$\text{Nu} = 48/11$.)

\end{thebibliography}

\end{document}
